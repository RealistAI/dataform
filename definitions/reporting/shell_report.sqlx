config { type: "view", 
	 assertions: {
	 	nonNull: ["Job", "Job_Color",]
                     }
       }

SELECT
	Job,
	Title,
	Start,
	DATE(Finish) AS Finish,
	Permit,
	Job_Color	
FROM
	${ref("shell_staging")}
ORDER BY
	Finish ASC
