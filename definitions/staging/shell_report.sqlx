config { type: "view" }

SELECT
  s.job,
  s.title,
  DATE(s.start) AS start,
  DATE(s.finish) AS finish,
  j.job_name,
  j.permit,
  j.job_color
FROM
  ${ref("schedule")} s
LEFT JOIN
  ${ref("jobsites")} j
ON
  s.job = j.job_name
ORDER BY
  DATE(s.finish)
