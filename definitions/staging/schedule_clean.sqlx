config { type: "table" }

SELECT
	job AS job_name,
	title,
	${phase_count.phase_count("title")} AS phase,
	completed,
	assigned_to,
	s.start as scheduled_start,
	s.end as scheduled_end
FROM
	${ref("schedule")} as s	
		

